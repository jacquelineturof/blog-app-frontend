import React from 'react'

const styles = {
    container: {
        height: '95vh',
        width: '100vw',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
}

const adminHome = () => {
    return (
        <section style = { styles.container }>
            I want to die
        </section>
    )
}

export default adminHome